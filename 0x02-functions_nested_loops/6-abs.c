#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n:value of interger
 * Return:Absolute value
 * Auth - OLUMOYIN JOSHUA
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
return (-n);
}
